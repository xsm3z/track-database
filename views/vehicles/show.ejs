<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <%- include('../partials/_navbar') %>
  <h1><%= vehicle.make %> <%= vehicle.model %></h1>
  <p>VIN: <%= vehicle.vin %></p>
  <p>Class: <%= vehicle.class %></p>
  <p>Number: <%= vehicle.number %></p>
  <a href="/drivers/<%= vehicle.driver._id %>/vehicles/<%= vehicle._id %>/edit">Edit Vehicle</a>
  <form action="/drivers/<%= vehicle.driver._id %>/vehicles/<%= vehicle._id %>?_method=DELETE" method="POST">
    <button type="submit">Delete Vehicle</button>
  </form>
  <a href="/drivers/<%= vehicle.driver._id %>/vehicles">Back to Vehicles</a>  
</body>
</html>